<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MomAuthMapper">

    <select id="findSetting" resultType="com.example.demo.model.MomAuthSetting">
        SELECT id, approver_code, secret, secret_initialized
        FROM mom_auth
        WHERE id = 1
    </select>

    <update id="updateSecret">
        UPDATE mom_auth
        SET secret = #{secret},
            secret_initialized = TRUE
        WHERE id = 1
    </update>

    <update id="resetApprover">
        UPDATE mom_auth
        SET approver_code = #{approverCode},
            secret = '',
            secret_initialized = FALSE
        WHERE id = 1
    </update>

</mapper>
